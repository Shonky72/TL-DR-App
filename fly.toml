app = 'tldr-discord-bot'
primary_region = 'ord'

[build]

[env]
  DATA_DIR = '/data'

[[mounts]]
  source = 'tldr_data'
  destination = '/data'

[deploy]
  strategy = 'immediate'

[[vm]]
  memory = '256mb'
  cpu_kind = 'shared'
  cpus = 1
